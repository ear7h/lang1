/*
pub enum Type {
    Int1,
    Int2,
    Int3,
    Int4,

    Uint1,
    Uint2,
    Uint3,
    Uint4,

    Bool,

    Struct(Vec<(String, Type)>),
    Enum(Vec<(String, Type)>),
    Tuple(Vec<Type>),
    Function(Box<Type>, Box<Type>),
    Ptr(Box<Type>),
}
*/
